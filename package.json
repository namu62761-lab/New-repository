{
  "name": "cc-patches",
  "version": "0.1.0",
  "description": "Runtime patches for minified Claude Code bundles",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "eval": "tsc && node dist/jailbreak/cli.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
